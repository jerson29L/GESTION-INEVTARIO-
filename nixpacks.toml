[phases.setup]
nixPkgs = ['nodejs_22']

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = ['npm install', 'npm run build:prod', 'cd backend && npm install']

[start]
cmd = 'cd backend && node server.js'
